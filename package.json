{
  "name": "jide-accounts-preview",
  "version": "0.5.0-preview",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.mjs",
    "preview": "node server.mjs --preview",
    "test": "node --test tests/*.test.mjs"
  }
}
